// QA-only for now — when this is promoted to master, point this at the
// production Function App (learnly-api.azurewebsites.net) instead.
export const API_BASE = "https://learnly-api-qa.azurewebsites.net"
